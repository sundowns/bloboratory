Enemy = Class {
    init = function(self, worldX, worldY, health)
        self.worldX = worldX
        self.worldY = worldY
        self.health = health
    end;
    update = function(self, dt)
    end;
    draw = function(self)
    end;
}