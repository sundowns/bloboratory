Enemy = Class {
    init = function(self, worldX, worldY)
        self.worldX = worldX
        self.worldY = worldY
    end;
    update = function(self, dt)
    end;
    draw = function(self)
    end;
}