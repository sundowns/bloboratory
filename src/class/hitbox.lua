Hitbox = Class {
    init = function(self, owner, type, x, y, width, height)
        self.owner = owner
        self.type = type
        self.x = x
        self.y = y
        self.width = width
        self.height = height
    end;
}